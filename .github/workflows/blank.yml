name: Build APK - Real Ads and Logo

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Unzip project
        run: |
          unzip mc-assets.zip
          if [ -d "mc-assets" ]; then mv mc-assets/* .; fi

      - name: Replace Ad IDs (Real Ads)
        run: |
          # REPLACE THE ID BELOW WITH YOUR REAL AD UNIT ID FROM ADMOB
          REAL_AD_ID="ca-app-pub-3303988035586005/9477951640"
          
          # This finds the Test Ad ID in any file and replaces it with your Real ID
          find . -type f \( -name "*.xml" -o -name "*.kt" -o -name "*.java" \) -exec sed -i "s/ca-app-pub-3940256099942544\/6300978111/$REAL_AD_ID/g" {} +

      - name: Replace App Logo
        run: |
          # This takes your uploaded 'my_logo.png' and forces it to be the app icon
          # We overwrite all sizes (hdpi, xhdpi, etc.) with your logo
          find app/src/main/res -name "ic_launcher.png" -exec cp my_logo.png {} \;
          find app/src/main/res -name "ic_launcher_round.png" -exec cp my_logo.png {} \;

      - name: Create Keystore & Config
        run: |
          keytool -genkey -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          find . -type d -name "app" -exec cp debug.keystore {}/debug.keystore \;
          echo '{"project_info":{"project_number":"123","project_id":"ai-studio-project"},"client":[{"client_info":{"mobilesdk_app_id":"1","android_client_info":{"package_name":"com.aistudio.mcassets.bxgvqy"}},"api_key":[{"current_key":"key"}],"services":{"ads_service":{"status":2}}}]}' > google-services.json
          find . -type d -name "app" -exec cp google-services.json {}/google-services.json \;

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Build Final APK
        env:
          JAVA_TOOL_OPTIONS: "-Djava.awt.headless=true"
        run: |
          find . -name "gradlew" -exec chmod +x {} +
          gradle assembleDebug --no-daemon -x test

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Final-AI-App
          path: "**/build/outputs/apk/debug/*.apk"
